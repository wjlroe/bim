[package]
name = "kilo"
version = "0.1.0"
authors = ["William Roe <git@wjlr.org.uk>"]

[dependencies]
term = "*"

[target.'cfg(unix)'.dependencies]
libc = "*"

[target.'cfg(windows)'.dependencies]
winapi = "*"
kernel32-sys = "*"
